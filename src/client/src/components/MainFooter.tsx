import {Layout} from "antd";

const {Footer} = Layout;

const MainFooter = () => {
    return (
        <Footer style={{
            textAlign: "center",
            padding: "14px",
        }}>
            Tender tracker ©2024
        </Footer>
    );
};

export default MainFooter